////////////////////////////////////////////////////////////////////////////////
- fix bfmax for N0>2
- improve get_coeff
- try "const" for inputs
- try "uint" instead of "int"
- bitslicing notation is ambiguous, # slices means:
  operand bit width (7) <- preferred
  ...
- in upc computation and update invert for loops to get single scan of col
- pad in update_counters_uint8
- use O3 instead of O2 when profiling with hotspot
- "preimage" initializaion from qcbits?
- use correct padding instead of PAD32 and PAD8
- __attribute__((noinline))
- LUT for count leading zeroes
////////////////////////////////////////////////////////////////////////////////
- "inline" always capitalized
- no AVX-512
- check that all test_utils used in bfmax are optimal
////////////////////////////////////////////////////////////////////////////////
+ faster upc computation in u8
+ lift_mul_dense_to_sparse_CT ignores rows after the first? -> no, s is shifted
+ remove "+1" in "BITSLICED_OPERAND_WIDTH" -> -1%
+ modular reduction using "min" instead of "cmpgt" + "blendv" -> -4%
+ try "alignas(32)" on inputs -> no performance difference
////////////////////////////////////////////////////////////////////////////////
